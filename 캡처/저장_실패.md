---
tags: [캡처]
date: 2023-10-17
---
# 저장 실패


내용:  
다음은 이미지에서 추출한 텍스트입니다:

```
저장 실패
노선 저장 중 오류가 발생했습니다.
(DB ID나 토큰, 권한, 테이블 기준속성('이름' 등)을 확인하세요)

에러: body failed validation. Fix one:
body.parent.page_id should be defined, instead was 'undefined'.
body.parent.database_id should be a valid uuid, instead was '정의되지 않음'.
body.parent.data_source_id should be defined, instead was 'undefined'.
body.parent.workspace should be defined, instead was 'undefined'.

확인
```
